# Dependencies
node_modules/
.pnpm-store/

# Build output / SSR bundles (TanStack Start + Nitro + Vite)
dist/
.output/
.nitro/
.tanstack/
.vite/

# TypeScript / tooling caches
*.tsbuildinfo
tsconfig.tsbuildinfo

# Vite temp config
vite.config.ts.timestamp-*
vite.config.js.timestamp-*

# Env (never commit real secrets; .env.example is fine to track)
.env
.env.*
!.env.example

# Logs / OS
*.log
.DS_Store

# NOTE: src/routeTree.gen.ts IS committed (generated by the TanStack Router plugin,
# kept in VCS to match the sibling Qeet consoles and to build without a codegen step).
