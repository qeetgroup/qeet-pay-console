# Qeet Pay console — copy to .env for local dev.
# Vite inlines VITE_* vars into the browser bundle at build time.

# Base URL of the Qeet Pay backend API (defaults to the dev backend on :4201).
VITE_API_URL=http://localhost:4201
