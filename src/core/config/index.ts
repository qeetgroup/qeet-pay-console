// Canonical config re-export.
// Active navigation config lives in src/config/navigation.tsx for backward compat.
export { navGroups } from "../../config/navigation";
export type { NavGroup, NavItem } from "../../config/navigation";
